# CfA platform — agent entry point

Read CLAUDE.md for this repository's operating rules, including public-data
boundaries, build verification, indexing, and scoped email-editing authority.
README.md contains architecture history; its old PoC/banner descriptions must be
read alongside the later corrections in CLAUDE.md and DECISIONS.md.

For local SageRock client coordination, when available, start at
`/mnt/d/dev/sagerock/clients/center-for-anthroposophy/README.md` and follow its
client constraints. That private hub is not a build dependency: do not copy its
contents into this public repository. If it is unavailable, use this repository's
public instructions and request any missing client context required for the task.

This entry point changes no publishing gates, deployment settings, or permissions.
